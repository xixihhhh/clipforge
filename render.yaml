services:
  - type: web
    name: daihuo-jianshou
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: PORT
        value: 3000
      - key: HOST
        value: 0.0.0.0
